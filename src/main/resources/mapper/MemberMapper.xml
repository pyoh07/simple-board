<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.pyoh07.simpleboard.mapper.MemberMapper">

    <insert id="insert" parameterType="com.pyoh07.simpleboard.domain.Member" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO member (username, password, email, created_at)
        VALUES (#{username}, #{password}, #{email}, NOW())
    </insert>

    <select id="findByUsername" parameterType="string" resultType="com.pyoh07.simpleboard.domain.Member">
        SELECT * FROM member WHERE username = #{username}
    </select>

</mapper>
